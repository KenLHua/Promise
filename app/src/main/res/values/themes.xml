<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Promise" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/lilac</item>
        <item name="colorOnPrimary">@color/purple_500</item>
        <!-- Secondary brand color. -->
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/pastel_blue</item>
        <!-- Customize your theme here. -->
        <item name="android:actionOverflowButtonStyle">@style/Overflow</item>

    </style>
    <style name="Theme.Promise.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.Promise.NoActionBar.Black">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/dark_grey</item>
    </style>
    <style name="EditTextTheme">
        <item name="colorPrimary">@color/grey</item>
    </style>
    <style name="Overflow" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name = "android:src">@drawable/ic_settings_24</item>
    </style>
    <style name="Theme.Promise.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.Promise.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.Button" parent = "@android:style/Widget.Button">
        <item name= "android:backgroundTint">@color/purple_500</item>

    </style>
</resources>